# MSSH Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个全平台 SSH 客户端桌面应用（类 Xshell），覆盖会话管理、多标签终端、端口转发、SFTP、密钥管理、外观定制、日志回放、宏命令。

**Architecture:** 事件驱动流式架构 — Go 后端通过 Wails v3 Bindings 提供 CRUD 服务，通过 Wails Events 推送终端 I/O 流到 React 前端 xterm.js 渲染。

**Tech Stack:** Go 1.24 + Wails v3 + React 19 + shadcn/ui + Tailwind CSS 4 + Vite 6 + xterm.js 5 + modernc.org/sqlite + zustand

## Global Constraints

- Go 模块根路径: `mssh`（`go.mod` 于项目根目录）
- 所有 Go 代码文件 ≤ 300 行，函数 ≤ 50 行，嵌套 ≤ 3 层，参数 ≤ 3 个，圈复杂度 ≤ 10
- 所有 Go 源码文件行覆盖率 ≥ 90%
- 每个 Go 源文件对应同级 `*_test.go` 测试文件
- 目录权限 0700，文件权限 0600，可执行文件权限 0700
- 禁止在 for 循环中使用 defer
- 使用 `_` 显式忽略无关紧要的错误
- 代码编写完毕后执行 `golangci-lint run` 和 `goimports-reviser` 格式化
- 测试后清理临时产物（二进制、测试数据等）
- Commit 格式: `<type>(scope): <summary>`，summary 使用中文动词开头、≤ 50 字
- 使用 modernc.org/sqlite（pure Go SQLite 驱动）
- 默认 Terminal 实例池上限 32

---

### Phase 1: 项目脚手架

#### Task 1.1: 初始化 Wails v3 项目

**Files:**
- Create: `go.mod`
- Create: `main.go`
- Create: `wails.json`
- Create: `taskfile.yaml`（构建任务编排）
- Create: `.golangci.yml`

**Interfaces:**
- Produces: `main.go` 中 `func main()` 调用 `wails.Run(&options.App{...})`
- Produces: `go.mod` 声明 `module mssh`，Go 1.24，依赖 `github.com/wailsapp/wails/v3`

- [ ] **Step 1: 创建 go.mod**

  执行:
  ```bash
  go mod init mssh
  ```
  EARS: The `go.mod` shall declare module `mssh` with Go version `1.24`.

- [ ] **Step 2: 创建 main.go**

  ```go
  // main.go
  package main

  import (
  	"log"

  	"github.com/wailsapp/wails/v3/pkg/application"
  )

  func main() {
  	app := application.New(application.Options{
  		Name:        "mssh",
  		Description: "A cross-platform SSH client",
  		Services: []application.Service{
  			// Phase 10-11 阶段逐步注入
  		},
  		Assets: application.AssetOptions{
  			Handler: application.AssetFileServer("./frontend/dist"),
  		},
  		Mac: application.MacOptions{
  			ApplicationShouldTerminateAfterLastWindowClosed: true,
  		},
  	})

  	app.NewWebviewWindowWithOptions(application.WebviewWindowOptions{
  		Title: "MSSH",
  		Width: 1280,
  		Height: 800,
  	})

  	err := app.Run()
  	if err != nil {
  		log.Fatal(err)
  	}
  }
  ```
  EARS: WHEN the application starts, the <system> shall create a Wails v3 app with a 1280x800 webview window titled "MSSH".

- [ ] **Step 3: 创建 wails.json**

  ```json
  {
    "name": "mssh",
    "outputfilename": "mssh",
    "frontend:dir": "frontend",
    "frontend:install": "npm install",
    "frontend:build": "npm run build",
    "frontend:dev:watcher": "npm run dev",
    "frontend:dev:serverUrl": "http://localhost:5173",
    "wailsjsdir": "frontend/src/lib/wails",
    "author": {
      "name": "",
      "email": ""
    }
  }
  ```
  EARS: The `wails.json` shall configure `frontend:dir` as `frontend` and `wailsjsdir` as `frontend/src/lib/wails`.

- [ ] **Step 4: 创建 .golangci.yml**

  ```yaml
  run:
    timeout: 5m
  linters:
    enable:
      - govet
      - staticcheck
      - errcheck
      - gosimple
      - unused
      - ineffassign
      - cyclop
      - funlen
      - gocognit
      - dupl
  linters-settings:
    cyclop:
      max-complexity: 10
    funlen:
      lines: 50
    gocognit:
      min-complexity: 10
  issues:
    exclude-use-default: false
  ```
  EARS: The linter configuration shall enforce cyclomatic complexity ≤ 10, function length ≤ 50 lines.

- [ ] **Step 5: 创建 taskfile.yaml**

  ```yaml
  version: '3'
  tasks:
    dev:
      desc: 启动开发模式
      cmds:
        - wails3 dev
    build:
      desc: 生产构建
      cmds:
        - wails3 build
    lint:
      desc: 运行 golangci-lint
      cmds:
        - golangci-lint run --timeout 5m ./...
    fmt:
      desc: 格式化代码
      cmds:
        - goimports-reviser -rm-unused -format ./...
    test:
      desc: 运行测试 + 覆盖率
      cmds:
        - go test -race -coverprofile=coverage.out -covermode=atomic ./...
        - go tool cover -func=coverage.out | grep total
    clean:
      desc: 清理临时产物
      cmds:
        - rm -f coverage.out
        - go clean -testcache
  ```
  EARS: The taskfile shall provide `dev`, `build`, `lint`, `fmt`, `test`, `clean` tasks.

- [ ] **Step 6: 添加 Go 依赖**

  ```bash
  go get github.com/wailsapp/wails/v3
  go get modernc.org/sqlite
  go get golang.org/x/crypto/ssh
  go get golang.org/x/crypto/argon2
  go get github.com/pkg/sftp
  go get github.com/zalando/go-keyring
  go mod tidy
  ```
  EARS: The `go.mod` shall contain all required dependencies listed in the spec.

- [ ] **Step 7: 验证构建**

  ```bash
  go build -o /dev/null ./...
  ```
  预期: 构建成功

- [ ] **Step 8: 验证 lint**

  ```bash
  task lint
  ```
  预期: 无错误

- [ ] **Step 9: Commit**

  ```bash
  git add -A
  git commit -m "chore: 初始化 Wails v3 项目脚手架"
  ```

---

### Phase 2: 前端环境搭建

#### Task 2.1: 初始化 Vite + React + TypeScript 项目

**Files:**
- Create: `frontend/package.json`
- Create: `frontend/vite.config.ts`
- Create: `frontend/tsconfig.json`
- Create: `frontend/index.html`
- Create: `frontend/src/main.tsx`
- Create: `frontend/src/App.tsx`
- Create: `frontend/src/styles/globals.css`

**Interfaces:**
- Produces: Vite dev server on `http://localhost:5173`
- Consumes: (none, independent)

- [ ] **Step 1: 创建 package.json**

  ```bash
  mkdir -p frontend && cd frontend && npm init -y
  ```
  EARS: The `package.json` shall define the frontend project with `type: "module"`.

- [ ] **Step 2: 安装核心依赖**

  ```bash
  cd frontend && npm install react@^19 react-dom@^19 @xterm/xterm@^5 @xterm/addon-fit @xterm/addon-webgl @xterm/addon-search @xterm/addon-unicode11 zustand@^5 lucide-react clsx tailwind-merge
  ```
  EARS: The frontend shall depend on React 19, xterm.js 5, zustand 5, and lucide-react.

- [ ] **Step 3: 安装开发依赖**

  ```bash
  cd frontend && npm install -D typescript@^5 vite@^6 @vitejs/plugin-react tailwindcss@^4 @tailwindcss/vite
  ```
  EARS: The frontend shall use Vite 6 as build tool with TypeScript 5 and Tailwind CSS 4.

- [ ] **Step 4: 创建 vite.config.ts**

  ```typescript
  import { defineConfig } from 'vite'
  import react from '@vitejs/plugin-react'
  import tailwindcss from '@tailwindcss/vite'

  export default defineConfig({
    plugins: [react(), tailwindcss()],
    server: {
      port: 5173,
      strictPort: true,
    },
  })
  ```
  EARS: The Vite config shall use React plugin, Tailwind CSS plugin, and strict port 5173.

- [ ] **Step 5: 创建 tsconfig.json**

  ```json
  {
    "compilerOptions": {
      "target": "ES2022",
      "lib": ["ES2022", "DOM", "DOM.Iterable"],
      "module": "ESNext",
      "moduleResolution": "bundler",
      "jsx": "react-jsx",
      "strict": true,
      "esModuleInterop": true,
      "skipLibCheck": true,
      "forceConsistentCasingInFileNames": true,
      "resolveJsonModule": true,
      "isolatedModules": true,
      "noEmit": true
    },
    "include": ["src"]
  }
  ```
  EARS: TypeScript config shall enforce strict mode with ES2022 target and bundler module resolution.

- [ ] **Step 6: 创建 index.html**

  ```html
  <!DOCTYPE html>
  <html lang="zh-CN">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>MSSH</title>
    </head>
    <body>
      <div id="root"></div>
      <script type="module" src="/src/main.tsx"></script>
    </body>
  </html>
  ```
  EARS: The HTML entry shall mount React at `#root` and load `/src/main.tsx`.

- [ ] **Step 7: 创建 main.tsx**

  ```typescript
  import React from 'react'
  import ReactDOM from 'react-dom/client'
  import App from './App'
  import './styles/globals.css'

  ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
      <App />
    </React.StrictMode>,
  )
  ```
  EARS: The main entry shall render `<App />` into `#root` with StrictMode.

- [ ] **Step 8: 创建 App.tsx + globals.css**

  ```typescript
  // src/App.tsx
  export default function App() {
    return (
      <div className="flex h-screen w-screen bg-background text-foreground">
        <p className="m-auto text-lg">MSSH</p>
      </div>
    )
  }
  ```
  ```css
  /* src/styles/globals.css */
  @import "tailwindcss";

  :root {
    --background: #0d1117;
    --foreground: #c9d1d9;
  }

  @theme {
    --color-background: var(--background);
    --color-foreground: var(--foreground);
  }

  body {
    margin: 0;
    overflow: hidden;
  }
  ```
  EARS: The root App component shall render a full-screen dark-themed placeholder.

- [ ] **Step 9: 安装 shadcn/ui**

  ```bash
  cd frontend && npx shadcn@latest init -d
  ```
  EARS: shadcn/ui shall be initialized with default configuration.

- [ ] **Step 10: 验证前端构建**

  ```bash
  cd frontend && npm run build
  ```
  预期: 构建成功，无 TypeScript 错误

- [ ] **Step 11: Commit**

  ```bash
  git add -A
  git commit -m "chore(frontend): 初始化 Vite + React + Tailwind + shadcn 前端脚手架"
  ```

---

### Phase 3: 数据模型

#### Task 3.1: 定义所有数据模型

**Files:**
- Create: `internal/model/session.go`
- Create: `internal/model/key.go`
- Create: `internal/model/macro.go`
- Create: `internal/model/tunnel.go`
- Create: `internal/model/theme.go`
- Create: `internal/model/setting.go`
- Create: `internal/model/log.go`
- Test: `internal/model/session_test.go`

**Interfaces:**
- Produces: `model.Session`, `model.SessionFolder`, `model.SSHKey`, `model.Macro`, `model.Tunnel`, `model.Theme`, `model.Setting`, `model.SessionLog`, `model.RecordingEntry`
- Consumes: (none)

- [ ] **Step 1: 创建 internal/model/session.go**

  ```go
  // internal/model/session.go
  package model

  import "time"

  type AuthMethod string

  const (
  	AuthPassword            AuthMethod = "password"
  	AuthKey                 AuthMethod = "key"
  	AuthAgent               AuthMethod = "agent"
  	AuthKeyboardInteractive AuthMethod = "keyboard-interactive"
  )

  type SessionFolder struct {
  	ID        int64     `json:"id"`
  	Name      string    `json:"name"`
  	ParentID  *int64    `json:"parent_id"`
  	SortOrder int       `json:"sort_order"`
  	CreatedAt time.Time `json:"created_at"`
  	UpdatedAt time.Time `json:"updated_at"`
  }

  type Session struct {
  	ID         int64      `json:"id"`
  	FolderID   *int64     `json:"folder_id"`
  	Name       string     `json:"name"`
  	Host       string     `json:"host"`
  	Port       int        `json:"port"`
  	Username   string     `json:"username"`
  	AuthMethod AuthMethod `json:"auth_method"`
  	Password   string     `json:"password,omitempty"`
  	KeyID      *int64     `json:"key_id,omitempty"`
  	KeepAlive  int        `json:"keep_alive"`
  	TermType   string     `json:"term_type"`
  	SortOrder  int        `json:"sort_order"`
  	CreatedAt  time.Time  `json:"created_at"`
  	UpdatedAt  time.Time  `json:"updated_at"`
  }
  ```
  EARS: The `Session` struct shall contain fields for host, port, username, auth method, keep-alive interval, and terminal type with JSON tags.

- [ ] **Step 2: 创建 internal/model/key.go**

  ```go
  // internal/model/key.go
  package model

  import "time"

  type KeyType string

  const (
  	KeyTypeRSA    KeyType = "rsa"
  	KeyTypeED25519 KeyType = "ed25519"
  	KeyTypeECDSA  KeyType = "ecdsa"
  )

  type SSHKey struct {
  	ID            int64     `json:"id"`
  	Name          string    `json:"name"`
  	Type          KeyType   `json:"type"`
  	PrivateKey    string    `json:"-"`
  	PublicKey     string    `json:"public_key"`
  	HasPassphrase bool      `json:"has_passphrase"`
  	CreatedAt     time.Time `json:"created_at"`
  }
  ```
  EARS: The `SSHKey` struct shall exclude `PrivateKey` from JSON serialization via `json:"-"`.

- [ ] **Step 3: 创建 internal/model/macro.go**

  ```go
  // internal/model/macro.go
  package model

  import "time"

  type Macro struct {
  	ID        int64     `json:"id"`
  	Name      string    `json:"name"`
  	Command   string    `json:"command"`
  	Shortcut  string    `json:"shortcut"`
  	DelayMs   int       `json:"delay_ms"`
  	SortOrder int       `json:"sort_order"`
  	CreatedAt time.Time `json:"created_at"`
  }
  ```
  EARS: The `Macro` struct shall contain fields for command text, optional keyboard shortcut, and delay in milliseconds.

- [ ] **Step 4: 创建 internal/model/tunnel.go**

  ```go
  // internal/model/tunnel.go
  package model

  import "time"

  type TunnelType string

  const (
  	TunnelLocal   TunnelType = "local"
  	TunnelRemote  TunnelType = "remote"
  	TunnelDynamic TunnelType = "dynamic"
  )

  type Tunnel struct {
  	ID         int64      `json:"id"`
  	SessionID  int64      `json:"session_id"`
  	Name       string     `json:"name"`
  	Type       TunnelType `json:"type"`
  	LocalHost  string     `json:"local_host,omitempty"`
  	LocalPort  int        `json:"local_port"`
  	RemoteHost string     `json:"remote_host,omitempty"`
  	RemotePort int        `json:"remote_port"`
  	CreatedAt  time.Time  `json:"created_at"`
  }
  ```
  EARS: The `Tunnel` struct shall support three types: local, remote, and dynamic forwarding.

- [ ] **Step 5: 创建 internal/model/theme.go**

  ```go
  // internal/model/theme.go
  package model

  import "time"

  type Theme struct {
  	ID        int64     `json:"id"`
  	Name      string    `json:"name"`
  	IsBuiltin bool      `json:"is_builtin"`
  	Config    string    `json:"config"`
  	CreatedAt time.Time `json:"created_at"`
  }
  ```
  EARS: The `Theme` struct shall store terminal appearance config as a JSON string in `Config`.

- [ ] **Step 6: 创建 internal/model/setting.go**

  ```go
  // internal/model/setting.go
  package model

  type Setting struct {
  	Key   string `json:"key"`
  	Value string `json:"value"`
  }
  ```
  EARS: The `Setting` struct shall use key-value pairs for flexible configuration storage.

- [ ] **Step 7: 创建 internal/model/log.go**

  ```go
  // internal/model/log.go
  package model

  import "time"

  type RecordType uint32

  const (
  	RecordStdout RecordType = 0
  	RecordStdin  RecordType = 1
  )

  type SessionLog struct {
  	ID        int64      `json:"id"`
  	SessionID *int64     `json:"session_id"`
  	StartedAt time.Time  `json:"started_at"`
  	EndedAt   *time.Time `json:"ended_at,omitempty"`
  	DataPath  string     `json:"data_path"`
  }

  type RecordingEntry struct {
  	Timestamp time.Duration `json:"timestamp"`
  	Type      RecordType    `json:"type"`
  	Data      []byte        `json:"data"`
  }
  ```
  EARS: The `SessionLog` model shall track recording sessions with start/end timestamps and a file path for binary recording data.

- [ ] **Step 8: 编写单元测试 internal/model/session_test.go**

  ```go
  package model

  import (
  	"encoding/json"
  	"testing"

  	"github.com/stretchr/testify/assert"
  	"github.com/stretchr/testify/require"
  )

  func TestSessionJSONSerialization(t *testing.T) {
  	s := Session{
  		Name:       "test-host",
  		Host:       "192.168.1.1",
  		Port:       22,
  		Username:   "root",
  		AuthMethod: AuthPassword,
  	}
  	data, err := json.Marshal(s)
  	require.NoError(t, err)
  	var decoded Session
  	err = json.Unmarshal(data, &decoded)
  	require.NoError(t, err)
  	assert.Equal(t, "test-host", decoded.Name)
  	assert.Equal(t, "192.168.1.1", decoded.Host)
  }

  func TestSSHKeyPrivateKeyExcluded(t *testing.T) {
  	k := SSHKey{
  		Name:       "my-key",
  		PrivateKey: "secret",
  		PublicKey:  "public",
  	}
  	data, err := json.Marshal(k)
  	require.NoError(t, err)
  	assert.NotContains(t, string(data), "secret")
  }

  func TestTunnelTypeConstants(t *testing.T) {
  	assert.Equal(t, TunnelType("local"), TunnelLocal)
  	assert.Equal(t, TunnelType("remote"), TunnelRemote)
  	assert.Equal(t, TunnelType("dynamic"), TunnelDynamic)
  }
  ```
  EARS: IF a Session is serialized to JSON, THEN it shall contain name, host, port, and username fields. IF an SSHKey is serialized, THEN the PrivateKey field shall be excluded. The TunnelType constants shall equal their respective string values.

- [ ] **Step 9: 运行测试**

  ```bash
  go test -v ./internal/model/...
  ```
  预期: 3 tests PASS

- [ ] **Step 10: Commit**

  ```bash
  git add -A
  git commit -m "feat(model): 定义数据模型 Session/SSHKey/Macro/Tunnel/Theme/Setting/Log"
  ```

---

### Phase 4: 数据库层

#### Task 4.1: SQLite 数据库初始化与迁移

**Files:**
- Create: `internal/store/db.go`
- Test: `internal/store/db_test.go`

**Interfaces:**
- Produces: `func OpenDB(dataDir string) (*sql.DB, error)` — 打开/创建加密数据库
- Produces: `func Migrate(db *sql.DB) error` — 执行 DDL 迁移
- Consumes: `model.*` (Phase 3)

- [ ] **Step 1: 创建 internal/store/db.go**

  ```go
  // internal/store/db.go
  package store

  import (
  	"database/sql"
  	"fmt"
  	"os"
  	"path/filepath"

  	_ "modernc.org/sqlite"
  )

  func OpenDB(dataDir string) (*sql.DB, error) {
  	_ = os.MkdirAll(dataDir, 0700)
  	dbPath := filepath.Join(dataDir, "mssh.db")
  	var err error
  	db, err := sql.Open("sqlite", dbPath+"?_pragma=journal_mode(WAL)&_pragma=busy_timeout(5000)")
  	if err != nil {
  		return nil, fmt.Errorf("open database: %w", err)
  	}
  	db.SetMaxOpenConns(1)
  	return db, nil
  }

  func Migrate(db *sql.DB) error {
  	migrations := []string{
  		`CREATE TABLE IF NOT EXISTS session_folders (
  			id         INTEGER PRIMARY KEY AUTOINCREMENT,
  			name       TEXT NOT NULL,
  			parent_id  INTEGER REFERENCES session_folders(id),
  			sort_order INTEGER NOT NULL DEFAULT 0,
  			created_at TEXT NOT NULL DEFAULT (datetime('now')),
  			updated_at TEXT NOT NULL DEFAULT (datetime('now'))
  		)`,
  		`CREATE TABLE IF NOT EXISTS sessions (
  			id         INTEGER PRIMARY KEY AUTOINCREMENT,
  			folder_id  INTEGER REFERENCES session_folders(id),
  			name       TEXT NOT NULL,
  			host       TEXT NOT NULL,
  			port       INTEGER NOT NULL DEFAULT 22,
  			username   TEXT NOT NULL,
  			auth_method TEXT NOT NULL CHECK(auth_method IN ('password','key','agent','keyboard-interactive')),
  			password   TEXT,
  			key_id     INTEGER REFERENCES ssh_keys(id),
  			keep_alive INTEGER NOT NULL DEFAULT 30,
  			term_type  TEXT NOT NULL DEFAULT 'xterm-256color',
  			sort_order INTEGER NOT NULL DEFAULT 0,
  			created_at TEXT NOT NULL DEFAULT (datetime('now')),
  			updated_at TEXT NOT NULL DEFAULT (datetime('now'))
  		)`,
  		`CREATE TABLE IF NOT EXISTS ssh_keys (
  			id             INTEGER PRIMARY KEY AUTOINCREMENT,
  			name           TEXT NOT NULL,
  			type           TEXT NOT NULL CHECK(type IN ('rsa','ed25519','ecdsa')),
  			private_key    TEXT NOT NULL,
  			public_key     TEXT,
  			has_passphrase INTEGER NOT NULL DEFAULT 0,
  			created_at     TEXT NOT NULL DEFAULT (datetime('now'))
  		)`,
  		`CREATE TABLE IF NOT EXISTS tunnels (
  			id          INTEGER PRIMARY KEY AUTOINCREMENT,
  			session_id  INTEGER NOT NULL REFERENCES sessions(id),
  			name        TEXT NOT NULL,
  			type        TEXT NOT NULL CHECK(type IN ('local','remote','dynamic')),
  			local_host  TEXT,
  			local_port  INTEGER,
  			remote_host TEXT,
  			remote_port INTEGER,
  			created_at  TEXT NOT NULL DEFAULT (datetime('now'))
  		)`,
  		`CREATE TABLE IF NOT EXISTS macros (
  			id         INTEGER PRIMARY KEY AUTOINCREMENT,
  			name       TEXT NOT NULL,
  			command    TEXT NOT NULL,
  			shortcut   TEXT,
  			delay_ms   INTEGER NOT NULL DEFAULT 0,
  			sort_order INTEGER NOT NULL DEFAULT 0,
  			created_at TEXT NOT NULL DEFAULT (datetime('now'))
  		)`,
  		`CREATE TABLE IF NOT EXISTS themes (
  			id         INTEGER PRIMARY KEY AUTOINCREMENT,
  			name       TEXT NOT NULL,
  			is_builtin INTEGER NOT NULL DEFAULT 0,
  			config     TEXT NOT NULL,
  			created_at TEXT NOT NULL DEFAULT (datetime('now'))
  		)`,
  		`CREATE TABLE IF NOT EXISTS settings (
  			key   TEXT PRIMARY KEY,
  			value TEXT NOT NULL
  		)`,
  		`CREATE TABLE IF NOT EXISTS session_logs (
  			id         INTEGER PRIMARY KEY AUTOINCREMENT,
  			session_id INTEGER REFERENCES sessions(id),
  			started_at TEXT NOT NULL,
  			ended_at   TEXT,
  			data_path  TEXT NOT NULL
  		)`,
  	}
  	for _, m := range migrations {
  		_, err := db.Exec(m)
  		if err != nil {
  			return fmt.Errorf("migration: %w", err)
  		}
  	}
  	return nil
  }
  ```
  EARS: The `OpenDB` function shall create the data directory with 0700 permissions and open a WAL-mode SQLite database. The `Migrate` function shall execute all CREATE TABLE IF NOT EXISTS statements idempotently.

- [ ] **Step 2: 创建 internal/store/db_test.go**

  ```go
  package store

  import (
  	"testing"

  	"github.com/stretchr/testify/assert"
  	"github.com/stretchr/testify/require"
  )

  func TestOpenDB(t *testing.T) {
  	tmpDir := t.TempDir()
  	db, err := OpenDB(tmpDir)
  	require.NoError(t, err)
  	defer db.Close()
  	assert.NotNil(t, db)
  	err = db.Ping()
  	assert.NoError(t, err)
  }

  func TestMigrate(t *testing.T) {
  	tmpDir := t.TempDir()
  	db, err := OpenDB(tmpDir)
  	require.NoError(t, err)
  	defer db.Close()
  	err = Migrate(db)
  	require.NoError(t, err)
  	// 二次执行应幂等
  	err = Migrate(db)
  	require.NoError(t, err)
  }

  func TestMigrateTablesExist(t *testing.T) {
  	tmpDir := t.TempDir()
  	db, _ := OpenDB(tmpDir)
  	defer db.Close()
  	_ = Migrate(db)
  	expected := []string{
  		"session_folders", "sessions", "ssh_keys", "tunnels",
  		"macros", "themes", "settings", "session_logs",
  	}
  	for _, table := range expected {
  		var count int
  		err := db.QueryRow(
  			"SELECT count(*) FROM sqlite_master WHERE type='table' AND name=?",
  			table,
  		).Scan(&count)
  		require.NoError(t, err)
  		assert.Equal(t, 1, count, "table %s should exist", table)
  	}
  }
  ```
  EARS: IF Migrate is called twice, THEN no error shall occur (idempotent). IF Migrate succeeds, THEN all 8 tables shall exist in sqlite_master.

- [ ] **Step 3: 运行测试**

  ```bash
  go test -v ./internal/store/...
  ```
  预期: PASS

- [ ] **Step 4: Commit**

  ```bash
  git add -A
  git commit -m "feat(store): 实现 SQLite 数据库初始化与 DDL 迁移"
  ```

#### Task 4.2: Session 存储 CRUD

**Files:**
- Create: `internal/store/session.go`
- Create: `internal/store/setting.go`
- Test: `internal/store/session_test.go`

**Interfaces:**
- Produces: `func CreateFolder(db, name, parentID) (SessionFolder, error)`, `func ListFolders(db) ([]SessionFolder, error)`, etc.
- Consumes: `model.Session`, `model.SessionFolder`, `OpenDB()` (Task 4.1)

- [ ] **Step 1: 创建 internal/store/session.go**

  ```go
  package store

  import (
  	"database/sql"
  	"fmt"
  	"time"

  	"mssh/internal/model"
  )

  func CreateFolder(db *sql.DB, name string, parentID *int64) (*model.SessionFolder, error) {
  	result, err := db.Exec(
  		"INSERT INTO session_folders (name, parent_id) VALUES (?, ?)",
  		name, parentID,
  	)
  	if err != nil {
  		return nil, fmt.Errorf("create folder: %w", err)
  	}
  	id, _ := result.LastInsertId()
  	return &model.SessionFolder{ID: id, Name: name, ParentID: parentID, CreatedAt: time.Now(), UpdatedAt: time.Now()}, nil
  }

  func ListFolders(db *sql.DB) ([]model.SessionFolder, error) {
  	rows, err := db.Query(
  		"SELECT id, name, parent_id, sort_order, created_at, updated_at FROM session_folders ORDER BY sort_order",
  	)
  	if err != nil {
  		return nil, fmt.Errorf("list folders: %w", err)
  	}
  	defer rows.Close()
  	var folders []model.SessionFolder
  	for rows.Next() {
  		var f model.SessionFolder
  		var createdAt, updatedAt string
  		err := rows.Scan(&f.ID, &f.Name, &f.ParentID, &f.SortOrder, &createdAt, &updatedAt)
  		if err != nil {
  			return nil, fmt.Errorf("scan folder: %w", err)
  		}
  		f.CreatedAt, _ = time.Parse("2006-01-02 15:04:05", createdAt)
  		f.UpdatedAt, _ = time.Parse("2006-01-02 15:04:05", updatedAt)
  		folders = append(folders, f)
  	}
  	if folders == nil {
  		folders = []model.SessionFolder{}
  	}
  	return folders, rows.Err()
  }

  func UpdateFolder(db *sql.DB, id int64, name string) error {
  	_, err := db.Exec(
  		"UPDATE session_folders SET name = ?, updated_at = datetime('now') WHERE id = ?",
  		name, id,
  	)
  	if err != nil {
  		return fmt.Errorf("update folder: %w", err)
  	}
  	return nil
  }

  func DeleteFolder(db *sql.DB, id int64) error {
  	_, err := db.Exec("DELETE FROM session_folders WHERE id = ?", id)
  	if err != nil {
  		return fmt.Errorf("delete folder: %w", err)
  	}
  	return nil
  }

  func CreateSession(db *sql.DB, s model.Session) (*model.Session, error) {
  	result, err := db.Exec(
  		`INSERT INTO sessions (folder_id, name, host, port, username, auth_method, password, key_id, keep_alive, term_type, sort_order)
  		 VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
  		s.FolderID, s.Name, s.Host, s.Port, s.Username, s.AuthMethod, s.Password, s.KeyID, s.KeepAlive, s.TermType, s.SortOrder,
  	)
  	if err != nil {
  		return nil, fmt.Errorf("create session: %w", err)
  	}
  	id, _ := result.LastInsertId()
  	s.ID = id
  	s.CreatedAt = time.Now()
  	s.UpdatedAt = time.Now()
  	return &s, nil
  }

  func ListSessions(db *sql.DB, folderID *int64) ([]model.Session, error) {
  	var rows *sql.Rows
  	var err error
  	if folderID != nil {
  		rows, err = db.Query(
  			`SELECT id, folder_id, name, host, port, username, auth_method, password, key_id, keep_alive, term_type, sort_order, created_at, updated_at
  			 FROM sessions WHERE folder_id = ? ORDER BY sort_order`, *folderID,
  		)
  	} else {
  		rows, err = db.Query(
  			`SELECT id, folder_id, name, host, port, username, auth_method, password, key_id, keep_alive, term_type, sort_order, created_at, updated_at
  			 FROM sessions ORDER BY sort_order`,
  		)
  	}
  	if err != nil {
  		return nil, fmt.Errorf("list sessions: %w", err)
  	}
  	defer rows.Close()
  	var sessions []model.Session
  	for rows.Next() {
  		var s model.Session
  		var createdAt, updatedAt string
  		err := rows.Scan(&s.ID, &s.FolderID, &s.Name, &s.Host, &s.Port, &s.Username, &s.AuthMethod, &s.Password, &s.KeyID, &s.KeepAlive, &s.TermType, &s.SortOrder, &createdAt, &updatedAt)
  		if err != nil {
  			return nil, fmt.Errorf("scan session: %w", err)
  		}
  		s.CreatedAt, _ = time.Parse("2006-01-02 15:04:05", createdAt)
  		s.UpdatedAt, _ = time.Parse("2006-01-02 15:04:05", updatedAt)
  		sessions = append(sessions, s)
  	}
  	if sessions == nil {
  		sessions = []model.Session{}
  	}
  	return sessions, rows.Err()
  }

  func UpdateSession(db *sql.DB, s model.Session) error {
  	_, err := db.Exec(
  		`UPDATE sessions SET folder_id=?, name=?, host=?, port=?, username=?, auth_method=?, password=?, key_id=?, keep_alive=?, term_type=?, sort_order=?, updated_at=datetime('now')
  		 WHERE id=?`,
  		s.FolderID, s.Name, s.Host, s.Port, s.Username, s.AuthMethod, s.Password, s.KeyID, s.KeepAlive, s.TermType, s.SortOrder, s.ID,
  	)
  	if err != nil {
  		return fmt.Errorf("update session: %w", err)
  	}
  	return nil
  }

  func DeleteSession(db *sql.DB, id int64) error {
  	_, err := db.Exec("DELETE FROM sessions WHERE id = ?", id)
  	if err != nil {
  		return fmt.Errorf("delete session: %w", err)
  	}
  	return nil
  }
  ```
  EARS: The CRUD functions shall return empty slices (not nil) when no records exist. All time parsing shall use the "2006-01-02 15:04:05" format.

- [ ] **Step 2: 创建 internal/store/setting.go**

  ```go
  package store

  import (
  	"database/sql"
  	"fmt"
  )

  func GetSetting(db *sql.DB, key string) (string, error) {
  	var value string
  	err := db.QueryRow("SELECT value FROM settings WHERE key = ?", key).Scan(&value)
  	if err == sql.ErrNoRows {
  		return "", nil
  	}
  	if err != nil {
  		return "", fmt.Errorf("get setting: %w", err)
  	}
  	return value, nil
  }

  func SetSetting(db *sql.DB, key, value string) error {
  	_, err := db.Exec(
  		"INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)",
  		key, value,
  	)
  	if err != nil {
  		return fmt.Errorf("set setting: %w", err)
  	}
  	return nil
  }
  ```
  EARS: The `GetSetting` function shall return empty string (not error) when the key does not exist. The `SetSetting` function shall upsert using INSERT OR REPLACE.

- [ ] **Step 3: 创建 internal/store/session_test.go**

  ```go
  package store

  import (
  	"testing"

  	"mssh/internal/model"

  	"github.com/stretchr/testify/assert"
  	"github.com/stretchr/testify/require"
  )

  func setupTestDB(t *testing.T) *sql.DB {
  	t.Helper()
  	db, err := OpenDB(t.TempDir())
  	require.NoError(t, err)
  	_ = Migrate(db)
  	t.Cleanup(func() { db.Close() })
  	return db
  }

  func TestCreateAndListFolders(t *testing.T) {
  	db := setupTestDB(t)
  	var parentID int64 = 0
  	folder, err := CreateFolder(db, "生产环境", &parentID)
  	require.NoError(t, err)
  	assert.Equal(t, "生产环境", folder.Name)
  	folders, err := ListFolders(db)
  	require.NoError(t, err)
  	assert.Len(t, folders, 1)
  	assert.Equal(t, "生产环境", folders[0].Name)
  }

  func TestCreateAndListSessions(t *testing.T) {
  	db := setupTestDB(t)
  	s := model.Session{
  		Name: "web-server", Host: "10.0.0.1", Port: 22, Username: "root",
  		AuthMethod: model.AuthPassword, Password: "encrypted", KeepAlive: 30, TermType: "xterm-256color",
  	}
  	created, err := CreateSession(db, s)
  	require.NoError(t, err)
  	assert.NotZero(t, created.ID)
  	sessions, err := ListSessions(db, nil)
  	require.NoError(t, err)
  	assert.Len(t, sessions, 1)
  	assert.Equal(t, "web-server", sessions[0].Name)
  }

  func TestUpdateAndDeleteSession(t *testing.T) {
  	db := setupTestDB(t)
  	s := model.Session{Name: "old", Host: "1.1.1.1", Port: 22, Username: "u", AuthMethod: model.AuthPassword, KeepAlive: 30}
  	created, _ := CreateSession(db, s)
  	created.Name = "new"
  	err := UpdateSession(db, *created)
  	require.NoError(t, err)
  	sessions, _ := ListSessions(db, nil)
  	assert.Equal(t, "new", sessions[0].Name)
  	err = DeleteSession(db, created.ID)
  	require.NoError(t, err)
  	sessions, _ = ListSessions(db, nil)
  	assert.Len(t, sessions, 0)
  }

  func TestGetSetSetting(t *testing.T) {
  	db := setupTestDB(t)
  	v, err := GetSetting(db, "nonexistent")
  	require.NoError(t, err)
  	assert.Equal(t, "", v)
  	err = SetSetting(db, "max_pool_size", "32")
  	require.NoError(t, err)
  	v, err = GetSetting(db, "max_pool_size")
  	require.NoError(t, err)
  	assert.Equal(t, "32", v)
  }
  ```
  EARS: IF a folder is created, THEN ListFolders shall return it. IF an empty query returns no rows, THEN the result shall be an empty slice, not nil. IF a setting key does not exist, THEN GetSetting shall return an empty string without error.

- [ ] **Step 4: 运行测试**

  ```bash
  go test -v -cover ./internal/store/...
  ```
  预期: PASS，覆盖率 > 80%

- [ ] **Step 5: Commit**

  ```bash
  git add -A
  git commit -m "feat(store): 实现 Session 文件夹/会话 CRUD 和 Settings 存储"
  ```

#### Task 4.3: Key/Macro/Tunnel/Theme/Log 存储

**Files:**
- Create: `internal/store/key.go`
- Create: `internal/store/macro.go`
- Create: `internal/store/tunnel.go`
- Create: `internal/store/theme.go`
- Create: `internal/store/log.go`
- Test: `internal/store/key_test.go`, `internal/store/macro_test.go`, `internal/store/tunnel_test.go`, `internal/store/theme_test.go`, `internal/store/log_test.go`

**Interfaces:**
- Produces: CRUD 函数 for `SSHKey`, `Macro`, `Tunnel`, `Theme`, `SessionLog`
- Consumes: `model.*` (Phase 3), `OpenDB()` (Task 4.1)

- [ ] **Step 1: 创建 internal/store/key.go (含完整 CRUD)**

  ```go
  package store

  import (
  	"database/sql"
  	"fmt"
  	"time"

  	"mssh/internal/model"
  )

  func CreateKey(db *sql.DB, k model.SSHKey) (*model.SSHKey, error) {
  	result, err := db.Exec(
  		"INSERT INTO ssh_keys (name, type, private_key, public_key, has_passphrase) VALUES (?, ?, ?, ?, ?)",
  		k.Name, k.Type, k.PrivateKey, k.PublicKey, k.HasPassphrase,
  	)
  	if err != nil {
  		return nil, fmt.Errorf("create key: %w", err)
  	}
  	id, _ := result.LastInsertId()
  	k.ID = id
  	k.CreatedAt = time.Now()
  	return &k, nil
  }

  func ListKeys(db *sql.DB) ([]model.SSHKey, error) {
  	rows, err := db.Query("SELECT id, name, type, private_key, public_key, has_passphrase, created_at FROM ssh_keys ORDER BY created_at DESC")
  	if err != nil {
  		return nil, fmt.Errorf("list keys: %w", err)
  	}
  	defer rows.Close()
  	var keys []model.SSHKey
  	for rows.Next() {
  		var k model.SSHKey
  		var createdAt string
  		err := rows.Scan(&k.ID, &k.Name, &k.Type, &k.PrivateKey, &k.PublicKey, &k.HasPassphrase, &createdAt)
  		if err != nil {
  			return nil, fmt.Errorf("scan key: %w", err)
  		}
  		k.CreatedAt, _ = time.Parse("2006-01-02 15:04:05", createdAt)
  		keys = append(keys, k)
  	}
  	if keys == nil {
  		keys = []model.SSHKey{}
  	}
  	return keys, rows.Err()
  }

  func DeleteKey(db *sql.DB, id int64) error {
  	_, err := db.Exec("DELETE FROM ssh_keys WHERE id = ?", id)
  	if err != nil {
  		return fmt.Errorf("delete key: %w", err)
  	}
  	return nil
  }

  func GetKey(db *sql.DB, id int64) (*model.SSHKey, error) {
  	var k model.SSHKey
  	var createdAt string
  	err := db.QueryRow(
  		"SELECT id, name, type, private_key, public_key, has_passphrase, created_at FROM ssh_keys WHERE id = ?", id,
  	).Scan(&k.ID, &k.Name, &k.Type, &k.PrivateKey, &k.PublicKey, &k.HasPassphrase, &createdAt)
  	if err != nil {
  		return nil, fmt.Errorf("get key: %w", err)
  	}
  	k.CreatedAt, _ = time.Parse("2006-01-02 15:04:05", createdAt)
  	return &k, nil
  }
  ```
  EARS: The key store shall support Create, List, Delete, and Get operations. GetKey shall return an error if the key does not exist.

- [ ] **Step 2: 创建 internal/store/macro.go / tunnel.go / theme.go / log.go**

  按照相同模式实现 Macro、Tunnel、Theme、SessionLog 的 CRUD 操作。

  EARS: Each store file shall implement Create, List, Update, Delete operations for its respective model.

- [ ] **Step 3: 编写全部存储层的单元测试**

  每个 `*_test.go` 覆盖 Create、List、Update、Delete 的完整流程。

  EARS: Each test file shall achieve ≥ 85% code coverage for its corresponding store file.

- [ ] **Step 4: 运行全量测试**

  ```bash
  go test -v -cover ./internal/store/...
  ```
  预期: 全部 PASS

- [ ] **Step 5: Commit**

  ```bash
  git add -A
  git commit -m "feat(store): 实现 Key/Macro/Tunnel/Theme/Log 存储 CRUD"
  ```

---

### Phase 5: 加密与密钥链

#### Task 5.1: AES 加密封装

**Files:**
- Create: `internal/crypto/aes.go`
- Test: `internal/crypto/aes_test.go`

**Interfaces:**
- Produces: `func Encrypt(plaintext []byte, key []byte) ([]byte, error)` — 返回 Base64 编码密文
- Produces: `func Decrypt(ciphertext []byte, key []byte) ([]byte, error)`
- Produces: `func DeriveKey(password string, salt []byte) ([]byte, []byte, error)` — Argon2id
- Produces: `func GenerateRandomBytes(n int) ([]byte, error)`

- [ ] **Step 1: 创建 internal/crypto/aes.go**

  ```go
  package crypto

  import (
  	"crypto/aes"
  	"crypto/cipher"
  	"crypto/rand"
  	"encoding/base64"
  	"fmt"

  	"golang.org/x/crypto/argon2"
  )

  const (
  	keyLen    = 32
  	saltLen   = 16
  	argonTime = 3
  	argonMem  = 64 * 1024
  )

  func GenerateRandomBytes(n int) ([]byte, error) {
  	b := make([]byte, n)
  	_, err := rand.Read(b)
  	if err != nil {
  		return nil, fmt.Errorf("generate random: %w", err)
  	}
  	return b, nil
  }

  func DeriveKey(password string, salt []byte) ([]byte, []byte, error) {
  	if salt == nil {
  		var err error
  		salt, err = GenerateRandomBytes(saltLen)
  		if err != nil {
  			return nil, nil, err
  		}
  	}
  	key := argon2.IDKey([]byte(password), salt, argonTime, argonMem, 1, keyLen)
  	return key, salt, nil
  }

  func Encrypt(plaintext, key []byte) ([]byte, error) {
  	block, err := aes.NewCipher(key)
  	if err != nil {
  		return nil, fmt.Errorf("create cipher: %w", err)
  	}
  	gcm, err := cipher.NewGCM(block)
  	if err != nil {
  		return nil, fmt.Errorf("create gcm: %w", err)
  	}
  	nonce, err := GenerateRandomBytes(gcm.NonceSize())
  	if err != nil {
  		return nil, err
  	}
  	ciphertext := gcm.Seal(nonce, nonce, plaintext, nil)
  	encoded := base64.StdEncoding.EncodeToString(ciphertext)
  	return []byte(encoded), nil
  }

  func Decrypt(encoded, key []byte) ([]byte, error) {
  	ciphertext, err := base64.StdEncoding.DecodeString(string(encoded))
  	if err != nil {
  		return nil, fmt.Errorf("decode base64: %w", err)
  	}
  	block, err := aes.NewCipher(key)
  	if err != nil {
  		return nil, fmt.Errorf("create cipher: %w", err)
  	}
  	gcm, err := cipher.NewGCM(block)
  	if err != nil {
  		return nil, fmt.Errorf("create gcm: %w", err)
  	}
  	nonceSize := gcm.NonceSize()
  	if len(ciphertext) < nonceSize {
  		return nil, fmt.Errorf("ciphertext too short")
  	}
  	nonce, ciphertext := ciphertext[:nonceSize], ciphertext[nonceSize:]
  	plaintext, err := gcm.Open(nil, nonce, ciphertext, nil)
  	if err != nil {
  		return nil, fmt.Errorf("decrypt: %w", err)
  	}
  	return plaintext, nil
  }
  ```
  EARS: The encrypt function shall return a Base64-encoded ciphertext. The decrypt function shall accept a Base64-encoded string and return plaintext. IF the ciphertext is too short, THEN Decrypt shall return an error.

- [ ] **Step 2: 创建 internal/crypto/aes_test.go**

  ```go
  package crypto

  import (
  	"testing"

  	"github.com/stretchr/testify/assert"
  	"github.com/stretchr/testify/require"
  )

  func TestEncryptDecrypt(t *testing.T) {
  	key, _, err := DeriveKey("test-password", nil)
  	require.NoError(t, err)
  	plaintext := []byte("sensitive data")
  	encrypted, err := Encrypt(plaintext, key)
  	require.NoError(t, err)
  	assert.NotEqual(t, plaintext, encrypted)
  	decrypted, err := Decrypt(encrypted, key)
  	require.NoError(t, err)
  	assert.Equal(t, plaintext, decrypted)
  }

  func TestDecryptWithWrongKey(t *testing.T) {
  	key1, _, _ := DeriveKey("pass1", nil)
  	key2, _, _ := DeriveKey("pass2", nil)
  	encrypted, _ := Encrypt([]byte("data"), key1)
  	_, err := Decrypt(encrypted, key2)
  	assert.Error(t, err)
  }

  func TestDeriveKeyDeterministic(t *testing.T) {
  	salt := []byte("1234567890123456")
  	key1, _, _ := DeriveKey("password", salt)
  	key2, _, _ := DeriveKey("password", salt)
  	assert.Equal(t, key1, key2)
  }

  func TestGenerateRandomBytes(t *testing.T) {
  	b, err := GenerateRandomBytes(32)
  	require.NoError(t, err)
  	assert.Len(t, b, 32)
  }

  func TestDecryptInvalidBase64(t *testing.T) {
  	key, _, _ := DeriveKey("pass", nil)
  	_, err := Decrypt([]byte("not-valid-base64!!!"), key)
  	assert.Error(t, err)
  }

  func TestEncryptEmptyData(t *testing.T) {
  	key, _, _ := DeriveKey("pass", nil)
  	encrypted, err := Encrypt([]byte{}, key)
  	require.NoError(t, err)
  	decrypted, _ := Decrypt(encrypted, key)
  	assert.Empty(t, decrypted)
  }
  ```
  EARS: IF data is encrypted with key A, THEN only key A shall decrypt it. IF the same password and salt are used, THEN DeriveKey shall produce identical keys. IF invalid Base64 is passed to Decrypt, THEN it shall return an error.

- [ ] **Step 3: 运行测试**

  ```bash
  go test -v -cover ./internal/crypto/...
  ```
  预期: PASS，覆盖率 > 90%

- [ ] **Step 4: Commit**

  ```bash
  git add -A
  git commit -m "feat(crypto): 实现 AES-256-GCM 加密与 Argon2id 密钥派生"
  ```

#### Task 5.2: 系统密钥链适配层

**Files:**
- Create: `internal/crypto/keychain.go`（接口定义）
- Create: `internal/crypto/keychain_default.go`（未检测到密钥链时的兜底）
- Create: `internal/crypto/keychain_test.go`

**Interfaces:**
- Produces: `type KeychainAdapter interface { Get(svc, acct) ([]byte, error); Set(svc, acct, []byte) error; Delete(svc, acct) error; IsAvailable() bool }`
- Produces: `func NewKeychainAdapter() KeychainAdapter` — 返回平台最优实现

- [ ] **Step 1: 创建 internal/crypto/keychain.go**

  ```go
  package crypto

  type KeychainAdapter interface {
  	Get(service, account string) ([]byte, error)
  	Set(service, account string, data []byte) error
  	Delete(service, account string) error
  	IsAvailable() bool
  }
  ```
  EARS: The KeychainAdapter interface shall define Get, Set, Delete, and IsAvailable methods for cross-platform credential storage.

- [ ] **Step 2: 创建 internal/crypto/keychain_default.go**

  ```go
  package crypto

  type defaultKeychain struct{}

  func (d *defaultKeychain) Get(_, _ string) ([]byte, error) { return nil, nil }
  func (d *defaultKeychain) Set(_, _ string, _ []byte) error  { return nil }
  func (d *defaultKeychain) Delete(_, _ string) error         { return nil }
  func (d *defaultKeychain) IsAvailable() bool                 { return false }

  func NewKeychainAdapter() KeychainAdapter {
  	return &defaultKeychain{}
  }
  ```
  EARS: The default adapter shall return IsAvailable() = false and implement no-op operations. Platform-specific adapters (keychain_windows.go, keychain_darwin.go, keychain_linux.go) shall use go-keyring for actual implementation.

- [ ] **Step 3: 创建 internal/crypto/keychain_test.go**

  ```go
  package crypto

  import (
  	"testing"

  	"github.com/stretchr/testify/assert"
  )

  func TestDefaultKeychainNotAvailable(t *testing.T) {
  	kc := NewKeychainAdapter()
  	assert.False(t, kc.IsAvailable())
  }

  func TestDefaultKeychainGetSet(t *testing.T) {
  	kc := NewKeychainAdapter()
  	err := kc.Set("svc", "acct", []byte("data"))
  	assert.NoError(t, err)
  	v, err := kc.Get("svc", "acct")
  	assert.NoError(t, err)
  	assert.Nil(t, v)
  }
  ```
  EARS: The default keychain adapter shall always return IsAvailable() = false and no-op for all operations.

- [ ] **Step 4: 运行测试**

  ```bash
  go test -v ./internal/crypto/...
  ```
  预期: PASS

- [ ] **Step 5: Commit**

  ```bash
  git add -A
  git commit -m "feat(crypto): 实现跨平台密钥链适配层接口与默认实现"
  ```

---

### Phase 6: SSH 引擎 — 连接与 PTY

#### Task 6.1: SSH 客户端连接管理

**Files:**
- Create: `internal/ssh/client.go`
- Create: `internal/ssh/key.go`
- Test: `internal/ssh/client_test.go`
- Test helper: `internal/ssh/testutil/server.go`

**Interfaces:**
- Produces: `func Connect(ctx context.Context, session model.Session, authMethods []ssh.AuthMethod) (*ssh.Client, error)`
- Consumes: `model.Session`, `model.SSHKey` (Phase 3), `internal/crypto` (Phase 5)

- [ ] **Step 1: 创建 test helper — 内存 SSH Server**

  ```go
  // internal/ssh/testutil/server.go
  package testutil

  import (
  	"crypto/rand"
  	"crypto/rsa"
  	"net"
  	"testing"

  	gossh "golang.org/x/crypto/ssh"
  )

  func NewMockServer(t *testing.T) (string, func()) {
  	t.Helper()
  	config := &gossh.ServerConfig{
  		NoClientAuth: true,
  	}
  	privateKey, _ := rsa.GenerateKey(rand.Reader, 2048)
  	signer, _ := gossh.NewSignerFromSigner(privateKey)
  	config.AddHostKey(signer)
  	listener, _ := net.Listen("tcp", "127.0.0.1:0")
  	go func() {
  		for {
  			conn, err := listener.Accept()
  			if err != nil {
  				return
  			}
  			_, chans, reqs, _ := gossh.NewServerConn(conn, config)
  			go gossh.DiscardRequests(reqs)
  			for ch := range chans {
  				if ch.ChannelType() == "session" {
  					channel, requests, _ := ch.Accept()
  					go func() {
  						for req := range requests {
  							switch req.Type {
  							case "shell":
  								req.Reply(true, nil)
  								channel.Write([]byte("mock> "))
  							case "pty-req":
  								req.Reply(true, nil)
  							default:
  								req.Reply(false, nil)
  							}
  						}
  					}()
  				}
  			}
  		}
  	}()
  	return listener.Addr().String(), func() { listener.Close() }
  }
  ```
  EARS: The mock SSH server shall accept connections without authentication, reply to "shell" and "pty-req" requests, and output "mock> " to the shell channel.

- [ ] **Step 2: 创建 internal/ssh/client.go**

  ```go
  package ssh

  import (
  	"context"
  	"fmt"
  	"net"

  	"mssh/internal/model"

  	gossh "golang.org/x/crypto/ssh"
  )

  type ClientWrapper struct {
  	Inner       *gossh.Client
  	keepAliveCtx    context.Context
  	keepAliveCancel context.CancelFunc
  }

  func Connect(ctx context.Context, s model.Session, auth []gossh.AuthMethod) (*ClientWrapper, error) {
  	config := &gossh.ClientConfig{
  		User:            s.Username,
  		Auth:            auth,
  		HostKeyCallback: gossh.InsecureIgnoreHostKey(),
  	}
  	addr := fmt.Sprintf("%s:%d", s.Host, s.Port)
  	dialer := &net.Dialer{Timeout: 10 * 1e9}
  	conn, err := dialer.DialContext(ctx, "tcp", addr)
  	if err != nil {
  		return nil, fmt.Errorf("dial %s: %w", addr, err)
  	}
  	sshConn, chans, reqs, err := gossh.NewClientConn(conn, addr, config)
  	if err != nil {
  		conn.Close()
  		return nil, fmt.Errorf("ssh handshake: %w", err)
  	}
  	client := gossh.NewClient(sshConn, chans, reqs)
  	kc, cancel := context.WithCancel(context.Background())
  	return &ClientWrapper{Inner: client, keepAliveCtx: kc, keepAliveCancel: cancel}, nil
  }

  func (c *ClientWrapper) Close() error {
  	c.keepAliveCancel()
  	return c.Inner.Close()
  }
  ```
  EARS: The `Connect` function shall dial the SSH server with a 10-second timeout and perform handshake. The `ClientWrapper` shall support graceful close with keep-alive context cancellation.

- [ ] **Step 3: 创建 internal/ssh/client_test.go**

  ```go
  package ssh

  import (
  	"context"
  	"testing"

  	"mssh/internal/model"
  	"mssh/internal/ssh/testutil"

  	gossh "golang.org/x/crypto/ssh"
  	"github.com/stretchr/testify/assert"
  	"github.com/stretchr/testify/require"
  )

  func TestConnect(t *testing.T) {
  	addr, cleanup := testutil.NewMockServer(t)
  	defer cleanup()
  	s := model.Session{Host: "127.0.0.1", Port: mustParsePort(addr), Username: "test"}
  	ctx := context.Background()
  	cw, err := Connect(ctx, s, nil)
  	require.NoError(t, err)
  	defer cw.Close()
  	assert.NotNil(t, cw.Inner)
  }

  func TestConnectInvalidHost(t *testing.T) {
  	s := model.Session{Host: "invalid.zzz.invalid.zzz", Port: 22, Username: "test"}
  	ctx, cancel := context.WithTimeout(context.Background(), 5*1e9)
  	defer cancel()
  	_, err := Connect(ctx, s, nil)
  	assert.Error(t, err)
  }

  func TestAuthMethodsBuilder(t *testing.T) {
  	passAuth := gossh.Password("secret")
  	assert.NotNil(t, passAuth)
  }
  ```
  EARS: IF a valid mock SSH server is running, THEN `Connect` shall succeed. IF the host is invalid, THEN `Connect` shall return an error.

- [ ] **Step 4: 运行测试**

  ```bash
  go test -v -cover ./internal/ssh/...
  ```
  预期: PASS

- [ ] **Step 5: Commit**

  ```bash
  git add -A
  git commit -m "feat(ssh): 实现 SSH 客户端连接管理与内存测试服务器"
  ```

#### Task 6.2: PTY 分配与终端 I/O 流

**Files:**
- Create: `internal/ssh/pty.go`
- Test: `internal/ssh/pty_test.go`

**Interfaces:**
- Produces: `func OpenPTY(c *ClientWrapper, termType string, cols, rows int) (*PTYSession, error)`
- Produces: `type PTYSession struct { Write(data []byte) (int, error); SetReadCallback(fn func([]byte)); Resize(cols, rows int) error; Close() error }`
- Consumes: `ClientWrapper` (Task 6.1)

- [ ] **Step 1: 创建 internal/ssh/pty.go**

  ```go
  package ssh

  import (
  	"fmt"
  	"io"

  	gossh "golang.org/x/crypto/ssh"
  )

  type PTYSession struct {
  	session     *gossh.Session
  	stdin       io.WriteCloser
  	readCb      func([]byte)
  	cancel      chan struct{}
  }

  func OpenPTY(c *ClientWrapper, termType string, cols, rows int) (*PTYSession, error) {
  	session, err := c.Inner.NewSession()
  	if err != nil {
  		return nil, fmt.Errorf("new session: %w", err)
  	}
  	modes := gossh.TerminalModes{
  		gossh.ECHO:  1,
  		gossh.TTY_OP_ISPEED: 14400,
  		gossh.TTY_OP_OSPEED: 14400,
  	}
  	if err := session.RequestPty(termType, rows, cols, modes); err != nil {
  		session.Close()
  		return nil, fmt.Errorf("request pty: %w", err)
  	}
  	stdin, err := session.StdinPipe()
  	if err != nil {
  		session.Close()
  		return nil, fmt.Errorf("stdin pipe: %w", err)
  	}
  	stdout, err := session.StdoutPipe()
  	if err != nil {
  		session.Close()
  		return nil, fmt.Errorf("stdout pipe: %w", err)
  	}
  	if err := session.Shell(); err != nil {
  		session.Close()
  		return nil, fmt.Errorf("start shell: %w", err)
  	}
  	ptys := &PTYSession{session: session, stdin: stdin, cancel: make(chan struct{}, 1)}
  	go ptys.readLoop(stdout)
  	return ptys, nil
  }

  func (p *PTYSession) readLoop(r io.Reader) {
  	buf := make([]byte, 4096)
  	for {
  		select {
  		case <-p.cancel:
  			return
  		default:
  		}
  		n, err := r.Read(buf)
  		if n > 0 {
  			data := make([]byte, n)
  			copy(data, buf[:n])
  			if p.readCb != nil {
  				p.readCb(data)
  			}
  		}
  		if err != nil {
  			return
  		}
  	}
  }

  func (p *PTYSession) SetReadCallback(fn func([]byte)) {
  	p.readCb = fn
  }

  func (p *PTYSession) Write(data []byte) (int, error) {
  	if p.stdin == nil {
  		return 0, fmt.Errorf("stdin not available")
  	}
  	return p.stdin.Write(data)
  }

  func (p *PTYSession) Resize(cols, rows int) error {
  	return p.session.WindowChange(rows, cols)
  }

  func (p *PTYSession) Close() error {
  	close(p.cancel)
  	return p.session.Close()
  }
  ```
  EARS: The `OpenPTY` function shall allocate a PTY with specified terminal type and dimensions, start a shell, and begin a read loop goroutine for stdout. The `readLoop` shall be cancellable via channel. `SetReadCallback` shall set a callback for received stdout data.

- [ ] **Step 2: 创建 internal/ssh/pty_test.go**

  ```go
  package ssh

  import (
  	"context"
  	"sync"
  	"testing"
  	"time"

  	"mssh/internal/model"
  	"mssh/internal/ssh/testutil"

  	"github.com/stretchr/testify/assert"
  	"github.com/stretchr/testify/require"
  )

  func TestOpenPTY(t *testing.T) {
  	addr, cleanup := testutil.NewMockServer(t)
  	defer cleanup()
  	s := model.Session{Host: "127.0.0.1", Port: mustParsePort(addr), Username: "test", TermType: "xterm-256color"}
  	ctx := context.Background()
  	cw, err := Connect(ctx, s, nil)
  	require.NoError(t, err)
  	defer cw.Close()
  	ptys, err := OpenPTY(cw, s.TermType, 80, 24)
  	require.NoError(t, err)
  	defer ptys.Close()
  	assert.NotNil(t, ptys)
  }

  func TestPTYWrite(t *testing.T) {
  	addr, cleanup := testutil.NewMockServer(t)
  	defer cleanup()
  	s := model.Session{Host: "127.0.0.1", Port: mustParsePort(addr), Username: "test", TermType: "xterm"}
  	ctx := context.Background()
  	cw, _ := Connect(ctx, s, nil)
  	defer cw.Close()
  	ptys, _ := OpenPTY(cw, s.TermType, 80, 24)
  	defer ptys.Close()
  	n, err := ptys.Write([]byte("ls\n"))
  	require.NoError(t, err)
  	assert.Equal(t, 3, n)
  }

  func TestPTYReadCallback(t *testing.T) {
  	addr, cleanup := testutil.NewMockServer(t)
  	defer cleanup()
  	s := model.Session{Host: "127.0.0.1", Port: mustParsePort(addr), Username: "test", TermType: "xterm"}
  	ctx := context.Background()
  	cw, _ := Connect(ctx, s, nil)
  	defer cw.Close()
  	ptys, _ := OpenPTY(cw, s.TermType, 80, 24)
  	defer ptys.Close()
  	var mu sync.Mutex
  	var received [][]byte
  	ptys.SetReadCallback(func(data []byte) {
  		mu.Lock()
  		received = append(received, data)
  		mu.Unlock()
  	})
  	time.Sleep(200 * time.Millisecond)
  	mu.Lock()
  	assert.NotEmpty(t, received)
  	mu.Unlock()
  }

  func TestPTYResize(t *testing.T) {
  	addr, cleanup := testutil.NewMockServer(t)
  	defer cleanup()
  	s := model.Session{Host: "127.0.0.1", Port: mustParsePort(addr), Username: "test", TermType: "xterm"}
  	ctx := context.Background()
  	cw, _ := Connect(ctx, s, nil)
  	defer cw.Close()
  	ptys, _ := OpenPTY(cw, s.TermType, 80, 24)
  	defer ptys.Close()
  	err := ptys.Resize(120, 40)
  	require.NoError(t, err)
  }
  ```
  EARS: IF a PTY session is opened, THEN Write shall send data to stdin. IF a read callback is set, THEN it shall receive stdout data from the mock server. Resize shall change terminal dimensions without error.

- [ ] **Step 3: 运行测试**

  ```bash
  go test -v -cover ./internal/ssh/...
  ```
  预期: PASS

- [ ] **Step 4: Commit**

  ```bash
  git add -A
  git commit -m "feat(ssh): 实现 PTY 分配与终端 I/O 流管理"
  ```

---

### Phase 7: SFTP 与端口转发引擎

#### Task 7.1: SFTP 客户端封装

**Files:**
- Create: `internal/ssh/sftp.go`
- Test: `internal/ssh/sftp_test.go`

**Interfaces:**
- Produces: `func OpenSFTP(cw *ClientWrapper) (*sftp.Client, error)`
- Produces: Upload/Download/ListDir/Delete/Mkdir/Rename with progress callback

- [ ] **Step 1: 创建 internal/ssh/sftp.go**

  ```go
  package ssh

  import (
  	"fmt"
  	"io"
  	"os"
  	"path/filepath"

  	"github.com/pkg/sftp"
  )

  type FileEntry struct {
  	Name    string `json:"name"`
  	Path    string `json:"path"`
  	Size    int64  `json:"size"`
  	IsDir   bool   `json:"is_dir"`
  	ModTime string `json:"mod_time"`
  }

  type ProgressFn func(bytesTransferred, totalBytes int64)

  func OpenSFTP(cw *ClientWrapper) (*sftp.Client, error) {
  	client, err := sftp.NewClient(cw.Inner)
  	if err != nil {
  		return nil, fmt.Errorf("open sftp: %w", err)
  	}
  	return client, nil
  }

  func ListDir(client *sftp.Client, path string) ([]FileEntry, error) {
  	entries, err := client.ReadDir(path)
  	if err != nil {
  		return nil, fmt.Errorf("list dir %s: %w", path, err)
  	}
  	var files []FileEntry
  	for _, e := range entries {
  		files = append(files, FileEntry{
  			Name:    e.Name(),
  			Path:    filepath.Join(path, e.Name()),
  			Size:    e.Size(),
  			IsDir:   e.IsDir(),
  			ModTime: e.ModTime().Format("2006-01-02 15:04:05"),
  		})
  	}
  	if files == nil {
  		files = []FileEntry{}
  	}
  	return files, nil
  }

  func UploadFile(client *sftp.Client, src, dst string, onProgress ProgressFn) error {
  	local, err := os.Open(src)
  	if err != nil {
  		return fmt.Errorf("open local: %w", err)
  	}
  	defer local.Close()
  	remote, err := client.Create(dst)
  	if err != nil {
  		return fmt.Errorf("create remote: %w", err)
  	}
  	defer remote.Close()
  	pw := &progressWriter{total: 0, onProgress: onProgress}
  	_, err = io.Copy(remote, io.TeeReader(local, pw))
  	if err != nil {
  		return fmt.Errorf("copy: %w", err)
  	}
  	return nil
  }

  type progressWriter struct {
  	total      int64
  	onProgress ProgressFn
  }

  func (pw *progressWriter) Write(p []byte) (int, error) {
  	n := len(p)
  	pw.total += int64(n)
  	if pw.onProgress != nil {
  		pw.onProgress(pw.total, 0)
  	}
  	return n, nil
  }
  ```
  EARS: The SFTP module shall provide ListDir (returns file entries with name, path, size, isDir, modTime), and UploadFile (with progress callback via io.TeeReader). Empty directory listings shall return an empty slice, not nil.

- [ ] **Step 2: 编写完整单元测试**

  EARS: Tests shall cover ListDir on root path, Upload with progress verification, and error cases for missing files. Target ≥ 85% coverage.

- [ ] **Step 3: 运行测试 + Commit**

  ```bash
  go test -v -cover ./internal/ssh/... && git add -A && git commit -m "feat(ssh): 实现 SFTP 文件列表与上传封装"
  ```

#### Task 7.2: 端口转发引擎

**Files:**
- Create: `internal/ssh/tunnel.go`
- Test: `internal/ssh/tunnel_test.go`

**Interfaces:**
- Produces: `func StartLocalForward(cw *ClientWrapper, localAddr, remoteAddr string) (net.Listener, error)`
- Produces: `func StartRemoteForward(cw *ClientWrapper, remoteAddr, localAddr string) error`
- Produces: `func StartDynamicForward(cw *ClientWrapper, localAddr string) (net.Listener, error)`

- [ ] **Step 1: 创建 internal/ssh/tunnel.go**

  ```go
  package ssh

  import (
  	"fmt"
  	"io"
  	"net"

  	"golang.org/x/crypto/ssh"
  )
  // Local/Remote/Dynamic 三种转发实现，返回 Listener 支持 Stop
  ```
  EARS: The tunnel module shall support local forwarding (listen local + dial remote via SSH), remote forwarding (register remote listener), and dynamic forwarding (SOCKS5 proxy).

- [ ] **Step 2: 编写测试 + Commit**

  EARS: Integration tests with mock SSH server validating port forwarding connections.

---

### Phase 8: 会话录制引擎

#### Task 8.1: 录制与回放

**Files:**
- Create: `internal/ssh/recorder.go`
- Create: `internal/ssh/player.go`
- Test: `internal/ssh/recorder_test.go`, `internal/ssh/player_test.go`

**Interfaces:**
- Produces: `type Recorder struct { Start(path, cols, rows, termType string) error; Write(data []byte, typ RecordType) error; Close() error }`
- Produces: `type Player struct { Open(path string) error; Read() ([]RecordingEntry, error); Seek(d time.Duration) error }`

- [ ] **Step 1: 创建 internal/ssh/recorder.go**

  实现二进制 `.msshlog` 格式的写入：
  - Header: 魔数 0x4D5353485F4C4F47 + version(4B) + cols(4B) + rows(4B) + term_type
  - Entry: timestamp_ms(8B) + type(4B: 0=stdout,1=stdin) + len(4B) + data

  EARS: The Recorder shall write a binary header with magic number "MSSH_LOG" and record each I/O entry with millisecond-precision timestamps.

- [ ] **Step 2: 创建 internal/ssh/player.go**

  实现 `.msshlog` 文件的读取解析：
  - 读取 Header 获取终端尺寸和类型
  - 读取所有 Entry 存储为 RecordingEntry 切片
  - 提供按时间戳回放的 Play 方法

  EARS: The Player shall parse the binary format and return all entries sorted by timestamp. IF the file magic number mismatches, THEN it shall return an error.

- [ ] **Step 3: 编写测试（录制→回放往返验证）**

  EARS: Test shall record mock I/O data, then play it back and verify data integrity match.

- [ ] **Step 4: Commit**

---

### Phase 9: 事件常量

#### Task 9.1: 事件名称常量

**Files:**
- Create: `pkg/event/event.go`
- Test: `pkg/event/event_test.go`

**Interfaces:**
- Produces: 所有事件名称常量 + payload 结构体

- [ ] **Step 1: 创建 pkg/event/event.go**

  ```go
  package event

  const (
  	TerminalOutput    = "terminal:output"
  	TerminalClosed    = "terminal:closed"
  	TransferProgress  = "file:progress"
  	TransferComplete  = "file:complete"
  	TransferError     = "file:error"
  	ConnectionState   = "session:state"
  	ConnectionError   = "session:error"
  	TunnelState       = "tunnel:state"
  )

  type TerminalOutputPayload struct {
  	TerminalID string `json:"terminal_id"`
  	Data       []byte `json:"data"`
  }

  type ConnectionStatePayload struct {
  	TerminalID string `json:"terminal_id"`
  	State      string `json:"state"`
  }

  type TransferProgressPayload struct {
  	TaskID  string  `json:"task_id"`
  	Percent float64 `json:"percent"`
  	Speed   int64   `json:"speed"`
  	ETA     int64   `json:"eta"`
  }
  ```
  EARS: Event constants shall follow the `domain:action` naming convention. Payload structs shall use snake_case JSON tags.

- [ ] **Step 2: Commit**

---

### Phase 10: SessionService + TerminalService

#### Task 10.1: SessionService 实现

**Files:**
- Create: `internal/service/session.go`
- Test: `internal/service/session_test.go`

**Interfaces:**
- Produces: `SessionService` struct with methods: ListFolders, CreateFolder, UpdateFolder, DeleteFolder, MoveFolder, ListSessions, CreateSession, UpdateSession, DeleteSession, MoveSession, Connect, Disconnect
- Consumes: `store.*` (Phase 4), `ssh.*` (Phase 6), `event.*` (Phase 9)

- [ ] **Step 1: 创建 internal/service/session.go**

  ```go
  package service

  import (
  	"context"
  	"fmt"
  	"sync"

  	"mssh/internal/model"
  	"mssh/internal/ssh"
  	"mssh/internal/store"
  	"mssh/pkg/event"

  	gossh "golang.org/x/crypto/ssh"
  )

  type SessionService struct {
  	db        *sql.DB
  	mu        sync.RWMutex
  	sessions  map[string]*ssh.ClientWrapper
  	eventBus  EventBus
  	keepAlive int
  }

  type EventBus interface {
  	Emit(name string, payload interface{})
  }

  func NewSessionService(db *sql.DB, bus EventBus, keepAlive int) *SessionService {
  	return &SessionService{db: db, sessions: make(map[string]*ssh.ClientWrapper), eventBus: bus, keepAlive: keepAlive}
  }

  func (s *SessionService) Connect(sessionID int64) (string, error) {
  	// 1. 查询 session
  	// 2. 构建 auth methods
  	// 3. ssh.Connect
  	// 4. 存入 map，生成 terminalID
  	// 5. 发送 EventConnectionState: "connected"
  	// 6. 返回 terminalID
  }

  func (s *SessionService) Disconnect(terminalID string) error {
  	// 1. 查找并关闭连接
  	// 2. 发送 EventConnectionState: "disconnected"
  	// 3. 从 map 中删除
  }
  ```
  EARS: The `Connect` method shall lookup the session from the database, build SSH auth methods, establish a connection, store it in the internal map keyed by terminalID, emit EventConnectionState, and return the terminalID. The `Disconnect` method shall close the SSH client, emit EventConnectionState("disconnected"), and remove from the map.

- [ ] **Step 2: 编写完整 CRUD + Connect/Disconnect 测试（使用 mock EventBus + 内存 SQLite）**

  EARS: Test shall cover all CRUD operations, connection success flow, connection to invalid host, double connect prevention, and disconnect cleanup. Target ≥ 90% coverage.

- [ ] **Step 3: Commit**

#### Task 10.2: TerminalService 实现

**Files:**
- Create: `internal/service/terminal.go`
- Test: `internal/service/terminal_test.go`

**Interfaces:**
- Produces: `TerminalService` struct with Write, Resize methods, pool management
- Consumes: `SessionService` (Task 10.1), `ssh.PTYSession` (Task 6.2), `event.*` (Phase 9)

- [ ] **Step 1: 创建 internal/service/terminal.go**

  ```go
  type TerminalService struct {
  	mu       sync.RWMutex
  	ptys     map[string]*ssh.PTYSession
  	eventBus EventBus
  	maxSize  int
  	lastUsed map[string]time.Time
  }

  func (t *TerminalService) Open(sessionID int64, cols, rows int) (string, error)
  func (t *TerminalService) Write(terminalID string, data []byte) error
  func (t *TerminalService) Resize(terminalID string, cols, rows int) error
  func (t *TerminalService) Close(terminalID string) error
  ```
  EARS: The `Open` method shall call ssh.OpenPTY, register the PTY in the pool, set read callback that emits EventTerminalOutput, and enforce the max pool size (default 32) via LRU eviction. The `Write` method shall forward data to the PTY stdin. The `Resize` method shall call PTY WindowChange.

- [ ] **Step 2: 编写测试（mock PTYSession + mock EventBus）**

  EARS: Test shall cover Open with pool limit enforcement, Write forwarding, Resize forwarding, Close cleanup, and LRU eviction when pool exceeds maxSize.

- [ ] **Step 3: Commit**

---

### Phase 11: FileService + TunnelService + KeyService + MacroService + ThemeService + LogService + SyncService

#### Task 11.1: FileService 实现

**Files:**
- Create: `internal/service/file.go`
- Test: `internal/service/file_test.go`

**Interfaces:**
- Produces: `FileService` with ListDir, Upload, Download, CancelTransfer, Delete, Mkdir, Rename
- Consumes: `ssh.*` (Phase 7), `event.*` (Phase 9)

- [ ] **Step 1: 实现 FileService**

  EARS: The `Upload` and `Download` methods shall accept a session ID, source/destination paths, and return a taskID. Progress shall be emitted via EventTransferProgress with percent, speed, and ETA. The `CancelTransfer` method shall cancel an ongoing transfer via context.

- [ ] **Step 2: 编写测试 + Commit**

#### Task 11.2: TunnelService 实现

**Files:**
- Create: `internal/service/tunnel.go`
- Test: `internal/service/tunnel_test.go`

- [ ] **Step 1: 实现 TunnelService**

  EARS: The `Start` method shall call ssh.StartLocalForward/StartRemoteForward/StartDynamicForward based on tunnel type and emit EventTunnelState. The `Stop` method shall close the listener and update state.

- [ ] **Step 2: 编写测试 + Commit**

#### Task 11.3: KeyService + MacroService + ThemeService + LogService + SyncService

**Files:**
- Create: `internal/service/key.go`, `internal/service/macro.go`, `internal/service/theme.go`, `internal/service/log.go`, `internal/service/sync.go`
- Test: 对应 `*_test.go`

- [ ] **Step 1-2: 逐一实现并测试每个 Service**

  EARS: Each service shall delegate to its corresponding store layer, performing encryption/decryption where applicable (KeyService encrypts private keys). LogService shall integrate with the Recorder. SyncService shall serialize sessions/keys/macros to JSON and back.

- [ ] **Step 3: Commit**

---

### Phase 12: 前端 App 布局

#### Task 12.1: 布局组件

**Files:**
- Create: `frontend/src/components/layout/Sidebar.tsx`
- Create: `frontend/src/components/layout/TabBar.tsx`
- Create: `frontend/src/components/layout/StatusBar.tsx`
- Modify: `frontend/src/App.tsx`

**Interfaces:**
- Produces: 三栏布局（Sidebar + MainPanel + StatusBar）

- [ ] **Step 1: 实现 App.tsx 根布局**

  ```tsx
  export default function App() {
    return (
      <div className="flex flex-col h-screen w-screen bg-background">
        <div className="flex flex-1 min-h-0">
          <Sidebar />
          <main className="flex-1 flex flex-col min-w-0">
            <TabBar />
            <div className="flex-1 min-h-0">{/* Tab content */}</div>
          </main>
        </div>
        <StatusBar />
      </div>
    )
  }
  ```
  EARS: The App component shall render a full-height flex layout with Sidebar (left), MainPanel (right, flex-1), and StatusBar (bottom).

- [ ] **Step 2: 实现 Sidebar（280px，标签切换：会话/宏）**

  EARS: The Sidebar shall have a fixed width of 280px with tab toggle buttons for sessions and macros.

- [ ] **Step 3: 实现 TabBar（标签列表 + 关闭按钮）**

  EARS: The TabBar shall display open tabs horizontally with active highlighting and close buttons.

- [ ] **Step 4: 实现 StatusBar（连接状态指示器）**

  EARS: The StatusBar shall display current connection info, transfer progress, and system indicators.

- [ ] **Step 5: Commit**

---

### Phase 13: 前端 Terminal Emulator

#### Task 13.1: Terminal 封装 + 实例池

**Files:**
- Create: `frontend/src/components/terminal/TerminalEmulator.tsx`
- Create: `frontend/src/hooks/useTerminal.ts`
- Modify: `frontend/src/store/appStore.ts`

**Interfaces:**
- Produces: `<TerminalEmulator terminalID={id} />` 组件
- Produces: `useTerminal(terminalID, containerRef)` hook

- [ ] **Step 1: 实现 useTerminal hook**

  - 从 zustand store 获取/创建 Terminal 实例
  - 挂载/卸载 DOM（仅移除 DOM，不销毁 Terminal）
  - 监听 EventTerminalOutput → term.write(data)
  - term.onData → Wails TerminalService.Write
  - ResizeObserver → Wails TerminalService.Resize
  - 池上限检查（当 terminalPool.size > maxPoolSize 时 LRU 淘汰）

  EARS: The `useTerminal` hook shall maintain a persistent xterm.js Terminal instance in the app store's pool. WHEN a tab is switched away, THEN the terminal DOM element shall be removed but the Terminal instance shall remain intact. WHEN a tab is switched back, THEN the Terminal shall be reattached to the DOM. IF the pool exceeds maxPoolSize, THEN the least recently used Terminal shall be evicted.

- [ ] **Step 2: 实现 TerminalEmulator 组件**

  EARS: The TerminalEmulator component shall create a div container, call useTerminal, and render the xterm.js instance.

- [ ] **Step 3: Commit**

---

### Phase 14: Session 管理 UI

#### Task 14.1: SessionTree + SessionDialog

**Files:**
- Create: `frontend/src/components/session/SessionTree.tsx`
- Create: `frontend/src/components/session/SessionDialog.tsx`
- Create: `frontend/src/hooks/useSession.ts`

- [ ] **Step 1: 实现 SessionTree**

  EARS: The SessionTree shall display folders as collapsible nodes and sessions as clickable items. Double-clicking a session shall call SessionService.Connect and open a new terminal tab.

- [ ] **Step 2: 实现 SessionDialog（新建/编辑会话表单）**

  EARS: The SessionDialog shall provide form fields for name, host, port, username, auth method (password/key/agent), keep-alive interval, and terminal type. Submit shall call SessionService.CreateSession/UpdateSession.

- [ ] **Step 3: 实现 useSession hook（调用 Wails bindings）**

- [ ] **Step 4: Commit**

---

### Phase 15: SFTP 文件面板 UI

#### Task 15.1: FilePanel + TransferProgress

**Files:**
- Create: `frontend/src/components/file/FilePanel.tsx`
- Create: `frontend/src/components/file/TransferProgress.tsx`
- Create: `frontend/src/hooks/useFileTransfer.ts`

- [ ] **Step 1: 实现 FilePanel（右侧滑出抽屉）**

  EARS: The FilePanel shall display a breadcrumb path bar, a file list table (name/size/modified), upload/download buttons, and a transfer queue list at the bottom.

- [ ] **Step 2: 实现 TransferProgress**

  EARS: The TransferProgress component shall display a progress bar with percentage, speed (KB/s), and ETA. It shall subscribe to EventTransferProgress/Complete/Error events.

- [ ] **Step 3: Commit**

---

### Phase 16: 设置/主题/密钥管理 UI

#### Task 16.1: SettingsDialog + ThemeEditor + KeyManager

**Files:**
- Create: `frontend/src/components/settings/SettingsDialog.tsx`
- Create: `frontend/src/components/settings/ThemeEditor.tsx`
- Create: `frontend/src/components/settings/KeyManager.tsx`
- Create: `frontend/src/hooks/useSettings.ts`

- [ ] **Step 1: 实现 SettingsDialog**

  EARS: The SettingsDialog shall provide tabs for General (keep-alive defaults, max pool size, terminal defaults), Appearance (active theme selection), and Advanced settings.

- [ ] **Step 2: 实现 ThemeEditor**

  EARS: The ThemeEditor shall use a color picker for each ANSI color slot (16 colors), plus foreground/background/cursor. Preview shall update in real-time.

- [ ] **Step 3: 实现 KeyManager**

  EARS: The KeyManager shall list all SSH keys, with buttons to Generate (select type + bits), Import (paste private key), Delete, and Export public key.

- [ ] **Step 4: Commit**

---

### Phase 17: 会话录制 + 宏 UI

#### Task 17.1: SessionLog + PlaybackTab + Macro UI

**Files:**
- Create: `frontend/src/components/terminal/SessionLog.tsx`
- Create: `frontend/src/components/terminal/PlaybackTab.tsx`
- Create: `frontend/src/components/session/QuickCommands.tsx`

- [ ] **Step 1: 实现录制控制**

  EARS: The terminal toolbar shall have a record button. WHEN clicked, it shall call LogService.StartRecording. WHEN clicked again, it shall call LogService.StopRecording.

- [ ] **Step 2: 实现 PlaybackTab**

  EARS: The PlaybackTab shall contain a readonly xterm.js instance + playback controls (play/pause, progress slider, speed selector). It shall call LogService.GetRecording and feed data via Player to the terminal.

- [ ] **Step 3: 实现 QuickCommands（宏列表）**

  EARS: The QuickCommands component in the sidebar shall list all macros with name and shortcut. Clicking a macro shall call MacroService.Execute on the active terminal.

- [ ] **Step 4: Commit**

---

### Phase 18: 端口转发 UI + 同步 UI

#### Task 18.1: TunnelManager + SyncPanel

**Files:**
- Create: `frontend/src/components/session/TunnelDialog.tsx`
- Create: `frontend/src/components/settings/SyncPanel.tsx`

- [ ] **Step 1: 实现 TunnelDialog**

  EARS: The TunnelDialog shall display a list of tunnels for a session with type selector (local/remote/dynamic), address/port inputs, and Start/Stop toggle buttons.

- [ ] **Step 2: 实现 SyncPanel**

  EARS: The SyncPanel shall provide Export (choose file path), Import (choose file), and Cloud Sync configuration (provider URL + credentials).

- [ ] **Step 3: Commit**

---

### Phase 19: Wails 集成与主入口组装

#### Task 19.1: 组装 main.go + 注入所有 Service

**Files:**
- Modify: `main.go`
- Create: `internal/app/app.go`

- [ ] **Step 1: 实现 app.go — 应用生命周期管理**

  ```go
  type App struct {
  	DB       *sql.DB
  	CryptoKey []byte
  	KC       crypto.KeychainAdapter
  	// Services
  	Session  *service.SessionService
  	Terminal *service.TerminalService
  	File     *service.FileService
  	Tunnel   *service.TunnelService
  	Key      *service.KeyService
  	Macro    *service.MacroService
  	Theme    *service.ThemeService
  	Log      *service.LogService
  	Sync     *service.SyncService
  }

  func New(config Config) (*App, error) {
  	// 1. 确定 dataDir (系统标准目录)
  	// 2. OpenDB + Migrate
  	// 3. 密钥链读取/主密码输入 → DeriveKey
  	// 4. 初始化所有 Service
  	// 5. 返回 App
  }
  ```
  EARS: The `New` function shall initialize the database, derive the master key, and wire all services together in dependency order.

- [ ] **Step 2: 修改 main.go 注入 Wails Services**

  EARS: The main.go shall call `app.New()`, register all Services in `application.Options.Services`, and add Wails menu items.

- [ ] **Step 3: 实现 Wails 前端生成的 Go→JS bindings**

  在每个 Service 结构体上添加 Wails v3 的 service export 方法。

- [ ] **Step 4: Commit**

---

### Phase 20: 集成测试 + CI + README

#### Task 20.1: 集成测试 + 覆盖率验证

- [ ] **Step 1: 运行全量测试**

  ```bash
  go test -race -coverprofile=coverage.out -covermode=atomic ./...
  go tool cover -func=coverage.out | grep total
  ```
  EARS: Total coverage shall be ≥ 90%.

- [ ] **Step 2: 修复覆盖率不足的模块**

- [ ] **Step 3: 运行 golangci-lint**

  ```bash
  golangci-lint run --timeout 5m ./...
  ```
  预期: 无警告错误

- [ ] **Step 4: 运行 goimports-reviser 格式化**

  ```bash
  goimports-reviser -rm-unused -format ./...
  ```

- [ ] **Step 5: 编写 README.md**

  EARS: The README shall contain a brief project description, tech stack, build/run instructions, and development workflow.

- [ ] **Step 6: 最终 Commit**

  ```bash
  git add -A
  git commit -m "chore: 集成测试、CI 配置、README 文档"
  ```
